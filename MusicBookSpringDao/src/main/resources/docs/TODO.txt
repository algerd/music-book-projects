
Два пути фильтрации:
1. Фильтрация из заранее загруженного массива данных. Требует полной загрузки всех данных.
    Минусы: сложность и громоздкость кода загрузки и слежения состояния загруженных данных.
        На хранение данных требует максимальной памяти.
    Плюсы: скорость обработки данных из памяти, а не из бд. Исключает дублирующих запросов 
        на загрузку из бд одних и тех же данных. Исключает дублирующее хранение одних и тех
        же данных в памяти.

2. Фильтрация из бд. Из бд загружаются только требуемые данные.
    Минусы: низкая скорость постоянных запросов к бд. Дублирующие запросы к бд к одним и тем же данным.
        Дублирующее хранение в памяти одних и тех же данных. Сложность и громоздкость запросов к бд.
        Сложность отслеживания изменений в бд.
    Плюсы: Использует минимальное количество памяти. Удобен для постраничной навигации и скроллируемой
        подгрузки данных.  

Сделать двойную сортировку в обзорных панелях: по рейтингу и по алфавиту.

Убрать растягивание таблиц, чтобы правильно отображались таблицы (без появляющихся прокруток и
смещённых шапак клолнок).

Доделать страницу Genre - вывод альбомов, песен и музыкантов (табы)

Добавить заглавки в табах : артисты жанра Rock. 
При бобавлении артиста, альбома или песни жанру или музыканту в диалоговом окне активировать
соответствующий чек-бокс.

Сортировать артистов у музыкантов(музыкантов у артистов) по имени и дате присоединения.

Убрать у Entity name и сделать каждой сущности свой equals() ???

При изменении альбома, артиста или музыканта, у них сначала удаляются все жанры, а потом заново добавляются.
Некрасиво сделано.

При создании дерева запрашивать у бд неполные данные (только id и name). При переходе по элементу дерева
догружать данные для элемента из бд.

Доделать меню: горизонтальное и вертикальное.

Сделать отдельные страницы всех жанров и интсрументов для возможности их редактирования.
Сделать для них поисковое поле.
Таблицы для жанров и артистов с полями:
    Genres:
    - Genre
    - Number of Artists
    - Number of Albums
    - Number of Songs
    - Number of Musicians
    - Average Rating of Artists
    - Average Rating of Albums
    - Average Rating of Songs
    - Average Rating of Musicians

    Instruments
    - Instrument
    - Number of Musicians
    - Average Rating

!!! Сделать именованные подготовленные запросы с более одного плейсхолдером.
    Разделить вызовы и запросы по классам.
    Сгруппировать Интерфейс, реализацию и запросы в папки репо.


