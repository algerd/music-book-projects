1)
    mvc.genre.controller.ArtistGenreTableController.setTableValue(ArtistGenreTableController.java:83):
    В initRepositoryListeners() вызывается changed()->setTableValue()->genrePaneController
    В bootstrap() переназначается genrePaneController, поэтому надо заново инициализировать слушателей:
    initRepositoryListeners()

    Проверить другие контроллеры на такую ситуацию - возникает при попытке редактирования.

    Пока не нашёл решение этой проблемы, располагать инициализатор слушателей там же, где они находятся
    в старом проекте(в методе bootstrap()), а не в инициализаторе контроллера.

3) (Баг из предыдущей версии): При добавлении песни, альбома или музыканта жанру из соответствующих
    таблиц страницы жанра, не выставляется по умолчанию поле жанра в селекторе жанра песни, альбома или музыканта

4) (Баг из предыдущей версии) При смене артиста у альбома (или альбома у песни) на страницах альбома(песни) не отображаются изменения,
 только при повторном вызове страницы этого альбома(песни)